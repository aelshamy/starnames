sudo: false
language: node_js
cache:
  directories:
    - node_modules
notifications:
  email: false
node_js:
  - '4'
before_install:
  - npm i -g npm@^2.0.0
  - pip install codecov
before_script:
  - npm prune
script:
  - npm run 
  - npm run check-coverage
  - npm run build
after_success:
  # - codecov
  - npm run report-coverage
  - npm run semantic-release
branches:
  except:
    - "/^v\\d+\\.\\d+\\.\\d+$/"
